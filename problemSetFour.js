var addNumbers = function (string) {
	for (i=0, i<string.length, i++) {
		if (i=NaN) {
		}
		else return i += newString
	}

}

var addNewString = function (newString) {
	for (i=0, i<newString.length, i++) {
		i
	}

}